[build]
builder = "dockerfile"
dockerfilePath = "Dockerfile.api"

[deploy]
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3

[healthcheck]
path = "/api/health"
intervalSeconds = 30
timeoutSeconds = 10
