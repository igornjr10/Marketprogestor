Estou começando o desenvolvimento da plataforma de gestão de tráfego pago.
Leia o CLAUDE.md antes de qualquer ação.

Sua tarefa nesta sessão é criar a fundação do projeto:

1. ESTRUTURA INICIAL
   - Configure o monorepo com Turborepo
   - Crie apps/web (Next.js 15), apps/api (NestJS 10), apps/workers (NestJS standalone)
   - Crie packages/database (Prisma), packages/types (tipos compartilhados)
   - Configure ESLint, Prettier, TypeScript em packages/config
   - .gitignore, .env.example, README.md inicial

2. INFRA LOCAL
   - docker-compose.yml com Postgres 16 e Redis 7
   - Scripts npm: dev, build, lint, typecheck, test
   - Husky + lint-staged para pre-commit

3. SCHEMA PRISMA INICIAL (packages/database/schema.prisma)
   Entidades mínimas para auth e tenancy:
   - Tenant (id, name, slug, createdAt, updatedAt)
   - User (id, email, passwordHash, name, role, tenantId, twoFactorSecret nullable)
   - Session (id, userId, refreshToken, expiresAt, ipAddress, userAgent)
   - AuditLog (id, userId, tenantId, action, entityType, entityId, before Json, after Json, createdAt)
   
   Inclua índices apropriados, particularmente em tenantId e em campos de busca.
   Crie middleware Prisma para tenantGuard.

4. AUTH BÁSICA (apps/api)
   - Módulo AuthModule
   - POST /auth/register (com criação de tenant)
   - POST /auth/login (retorna access + refresh token)
   - POST /auth/refresh
   - POST /auth/logout
   - Guard JWT
   - Hash de senha com argon2 (NÃO bcrypt)

5. LAYOUT BASE FRONTEND (apps/web)
   - Página de login funcional
   - Página de registro funcional
   - Dashboard vazio com sidebar (placeholders) e header
   - Rotas protegidas via middleware Next.js
   - Tema claro/escuro com next-themes
   - shadcn/ui instalado e configurado

CRITÉRIO DE ACEITAÇÃO:
- npm run dev levanta tudo (web na 3000, api na 3001, workers ouvindo)
- Consigo registrar um usuário, fazer login, ver dashboard vazio
- Logout funciona
- Lint e typecheck passam
- Pelo menos 5 testes unitários no AuthService

Pergunte se tiver dúvida ANTES de começar. Não improvise estrutura.