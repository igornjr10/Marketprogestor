{
  "buildCommand": "cd ../.. && pnpm --filter @marketproads/types build 2>/dev/null || true && cd apps/web && pnpm build",
  "outputDirectory": "apps/web/.next",
  "installCommand": "pnpm install --frozen-lockfile",
  "framework": "nextjs",
  "rootDirectory": "apps/web"
}
